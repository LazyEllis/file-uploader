<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Uploader | <%= file.name %></title>
    <%- include("partials/styles", { style: "file" }) %>
  </head>
  <body>
    <main>
      <h1>File Details</h1>
      <dl>
        <div>
          <dt>File Name</dt>
          <dd><%= file.name %></dd>
        </div>

        <div>
          <dt>Size</dt>
          <dd><%= file.size %></dd>
        </div>

        <div>
          <dt>Upload Time</dt>
          <dd><%= file.createdAt %></dd>
        </div>

        <% if (file.folder) { %>
          <div>
            <dt>Folder</dt>
            <dd>
              <a href="/folders/<%= file.folder.id %>" class="link-text"><%= file.folder.name %></a>
            </dd>
          </div>
        <% } %>
        
        <div>
          <dt>Actions</dt>
          <dd>
            <ul class="actions">
              <li><a href="/files/<%= file.id %>/download" class="button">Download</a></li>
              <li><a href="/files/<%= file.id %>/edit" class="button">Rename</a></li>
              <li>
                <form action="/files/<%= file.id %>/delete" method="post">
                  <button type="submit" class="button danger">Delete</button>
                </form>
              </li>
            </ul>
          </dd>
        </div>
      </dl>
    </main>
  </body>
</html>
